export enum Collection {
  InstrumentRegistration = 'instrument_registration',
  development = 'development',
  production = 'productiion',
}

export enum Document {
  FieldsBlocks = 'fields_blocks',
  InstrumentsGroups = 'instruments_groups',
  RegisteredAssets = 'registered_assets',
  RegisteredForms = 'registered_forms',
  SelectOptions = 'select_options',
}

export enum SubCollection {
  Fields = 'fields',
  Blocks = 'blocks',
  Instruments = 'instruments',
  Groups = 'groups',
  Options = 'options',
  Assets = 'assets',
  Forms = 'forms',
}
